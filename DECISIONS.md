# DECISIONS

## D-001 原生微信端

使用 WXML/WXSS/JavaScript，不引入 Taro、uni-app、React 或 Vue。理由：满足微信能力、广告和相册接口的直接兼容要求。

## D-002 广告绑定下载而非解析

解析与预览免费；保存动作检查下载权益。完整观看激励广告后，服务端按当前时间授予 24 小时下载权益，不叠加。理由：与用户提供的目标录屏一致，先展示价值再触发广告，降低首屏流失。

## D-003 单机 V1 持久任务与媒体会话

用户、下载权益、广告事件、解析任务和媒体会话统一存入 SQLite；媒体 token 只保存 SHA-256 摘要，媒体文件只保存 `TEMP_DIR` 下相对路径。单节点使用一个 Uvicorn worker并开启 SQLite WAL；开发默认 2 个任务 worker、同一用户最多 2 个活动任务，FFmpeg 媒体处理保持单路。生产 2 核 4GB 示例使用 1 个任务 worker，允许第二个任务排队，升级服务器后再调高。服务重启后安全任务重新排队，结果和记录默认保留 24 小时，随后自动清理。后续多实例再迁移 PostgreSQL/Redis 与对象存储。

## D-004 能力型媒体 token

预览和下载 URL 使用 256 位随机 token。token 只映射服务端已校验媒体，不包含上游 URL，不提供 `/proxy?url=` 形式接口。

## D-005 平台接入顺序

Generic → Bilibili → 微博 → 小红书 → 抖音 → 快手。公开路径不可稳定工作时返回规范化降级错误，不使用 Cookie 或访问控制绕过。

## D-006 相册格式

V1 下载结果限定 MP4。微信相册保存只接受本地 MP4，遇到仅有 WebM/MKV/MOV 的公开资源时返回 `MEDIA_FORMAT_UNSUPPORTED`，不把无法保存的文件伪装为成功。

## D-007 DNS 重绑定防护

普通请求继续使用 httpx；外部页面/媒体安全客户端在生产网络路径使用 aiohttp 自定义解析器，把连接固定到已校验的公网 IP，同时保留 TLS 主机名验证。每一跳重定向重新解析并复检，测试注入路径继续使用 httpx MockTransport。

## D-008 画质选择与微信文件边界

首页不预选画质；解析器返回真实可用源列表，结果页展示最终实际分辨率、编码和大小，并允许用户切换源。服务器允许暂存不超过 2GiB 的公开源文件；源文件超过成品边界或编码不兼容时，自动按时长计算目标码率，必要时逐级降到可交付档位，并转为 H.264 + AAC 单一 MP4。自动压缩结果明确标注实际结果，不冒充原视频。

保留 180MiB 成品上限是因为 `wx.downloadFile` 单次最大 200MB，`wx.saveVideoToPhotosAlbum` 需要本地 MP4。后端可分块下载、断线续传、合并或压缩大源文件，但交付给微信的最终文件仍必须留在可靠边界内；180MiB 为封装增长和机型差异预留空间，不能靠伪造大小或分片绕过客户端限制。

## D-009 首版生产免费模式

首版使用 `DOWNLOAD_ACCESS_MODE=free`，登录用户可直接保存，前端不初始化或展示广告。原有广告和 24 小时权益能力保留在 `rewarded_ad` 模式中，后续商业化无需重建数据结构，但广告不再阻塞当前上线。
