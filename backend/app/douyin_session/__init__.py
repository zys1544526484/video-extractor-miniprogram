"""Optional, isolated operator-session proof of concept for Douyin.

The main API deliberately does not import or invoke this package.  Playwright
is loaded only by the explicit bootstrap/worker entry points.
"""
