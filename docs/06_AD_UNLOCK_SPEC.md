# 广告与下载权益

- 展示型广告只承担持续曝光，不阻塞主流程。
- 激励广告只在用户首次保存且下载权益无效时出现。
- `RewardedVideoAd.onClose` 只有 `isEnded === true` 才调用服务端完成接口。
- 服务端计算 `unlock_until = server_now + 24h`，有效期内重复事件不续期。
- 广告结束后自动继续此前挂起的保存动作；中途关闭则不下载。
- Mock 广告必须明显属于开发模式，生产配置出现 Mock 时启动失败。

