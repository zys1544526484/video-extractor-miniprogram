# AGENTS.md — 视频提取小程序工程守则

本仓库实现微信原生小程序与 FastAPI 后端。开始修改前先阅读本文件、`docs/00_MASTER_EXECUTION.md` 以及当前任务相关规范。

## 不可违反的边界

- 只处理公开可访问、用户有权保存的内容。
- 不导入用户 Cookie，不模拟登录，不绕过验证码、风控、付费、私密、地域、年龄或 DRM。
- “原始媒体”不代表擦除作者烧录在画面里的水印。
- 媒体代理只接受服务端签发的短期 token，禁止任意 URL 开放代理。
- AppSecret、session_key、数据库密码和完整 Authorization 不得进入代码或日志。
- Mock 登录、Mock 广告和 Mock API 只能用于开发环境，生产启动必须拒绝 Mock 配置。

## 产品闭环

首版闭环为：粘贴分享文案 → 免费解析和预览 → 登录用户直接下载并保存到相册。广告与 24 小时权益代码保留在 `rewarded_ad` 模式中，但首版生产配置固定使用 `free`，不得渲染广告或要求广告单元。

## 技术栈

- 微信原生小程序：WXML、WXSS、JavaScript、微信原生 API。
- 后端：Python 3.12、FastAPI、Pydantic v2、SQLAlchemy 2、SQLite、httpx、pytest、ruff。
- yt-dlp/ffmpeg 只能用于公开媒体的标准提取、重封装或音视频合并。

## 阶段完成条件

对应代码和测试存在，实际检查通过，安全边界未放宽，`STATUS.md` 与 `BLOCKERS.md` 已更新。未真机、未部署或未使用真实凭证的项目必须标记 `NOT VERIFIED`。

## 变更、测试与提交

- 每次改动都必须同步新增或更新相关测试；纯文档改动至少运行与其影响范围对应的现有验证。
- 向用户交付前必须确保所有相关测试和验证全部通过，不得把未验证结果描述为已通过。
- 每次改动完成后必须创建对应的 Git commit，提交信息应准确说明本次变更，确保后续可追踪和回滚。

## 长期协作协议（ChatGPT 决策 / Codex 开发 / GitHub 中转）

- 禁止直接在 `main` 分支开发；`main` 只用于已审查、可合并的基线。
- 一个任务使用一个以 `codex/` 开头的独立分支，分支名应能表达任务目的。
- 每完成一个可以独立验证的小步骤，都必须运行与该步骤相关的测试或检查。
- 测试完成后，立即更新 `docs/HANDOFF.md`，记录变更、验证结果、未验证项和下一步建议。
- 每个可独立回滚的步骤单独创建一个 commit，并在 commit 后立即 push 到对应远程分支。
- 禁止 force push（包括 `--force`、`--force-with-lease` 及等效操作）。
- 未经用户明确允许，不得合并到 `main`，不得将 Draft PR 标记为 ready 或代替用户完成合并决策。
- 不得提交 `.env`、Token、密钥、密码、账号信息、完整 Authorization、Cookie、缓存、临时文件或其他敏感文件；不确定时先停止并报告。
- 如果 push 失败，必须停止后续写操作并报告真实错误；不得声称已经推送成功。
- 每次完成后必须报告仓库、分支、提交 SHA、PR 地址、测试结果和下一步建议。
